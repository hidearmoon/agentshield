{
  "id": "agentguard",
  "name": "AgentGuard Security Guard",
  "version": "0.1.0",
  "description": "Runtime security layer — inspects every tool call for intent consistency, trust violations, and injection attacks before execution.",
  "author": "AgentGuard Contributors",
  "license": "Apache-2.0",
  "homepage": "https://github.com/hidearmoon/agentguard",
  "entry": "dist/index.js",
  "capabilities": ["hooks"],
  "config": {
    "type": "object",
    "properties": {
      "coreUrl": {
        "type": "string",
        "default": "http://localhost:8000",
        "description": "URL of the AgentGuard core engine"
      },
      "apiKey": {
        "type": "string",
        "description": "AgentGuard API key"
      },
      "agentId": {
        "type": "string",
        "default": "openclaw",
        "description": "Agent identifier for trace grouping"
      },
      "trustMapping": {
        "type": "object",
        "default": {
          "user_input": "VERIFIED",
          "whatsapp": "EXTERNAL",
          "telegram": "EXTERNAL",
          "discord": "EXTERNAL",
          "slack": "INTERNAL",
          "email": "EXTERNAL",
          "web": "EXTERNAL",
          "system": "TRUSTED"
        },
        "description": "Map OpenClaw channel sources to AgentGuard trust levels"
      },
      "blockMessage": {
        "type": "string",
        "default": "This action was blocked by AgentGuard security policy.",
        "description": "Message shown to the user when a tool call is blocked"
      },
      "confirmTimeout": {
        "type": "number",
        "default": 30000,
        "description": "Timeout in ms for REQUIRE_CONFIRMATION decisions"
      }
    },
    "required": ["apiKey"]
  }
}
